{
  "name": "@throttr/typescript",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "test": "vitest --run",
    "test:watch": "vitest",
    "test:coverage": "vitest --run --coverage"
  },
  "devDependencies": {
    "@types/node": "^22.15.2",
    "@vitest/coverage-v8": "3.1.2",
    "c8": "^10.1.3",
    "typescript": "^5.8.3",
    "vitest": "^3.1.2"
  }
}
